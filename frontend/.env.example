# Chi can khi chay SPA TACH ROI entrypoint cong khai.
# Mac dinh (xem src/config.ts) da la same-origin: API_BASE_URL="/api", WS origin=window.location.origin.
# `npm run dev` da co proxy /api va /ws trong vite.config.ts nen thuong khong can file .env nao.
# File nay chi chua placeholder, khong chua secret.

# VITE_API_BASE_URL=http://localhost:8080/api
# VITE_NOTIFICATION_WS_URL=http://localhost:8080
