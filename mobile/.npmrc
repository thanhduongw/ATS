# TypeScript 6 (Expo SDK 57 default) vs openapi-typescript@7 peer "typescript@^5.x".
# Verified working: openapi-typescript 7.13.0 generates correct output under TS 6.
# Without this, a plain `npm install` fails on ERESOLVE.
legacy-peer-deps=true
