import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function JobsScreen() {
  return <PlaceholderScreen title="Việc làm" note="B1 — danh sách tin tuyển dụng (ngày 3–5)." />;
}
