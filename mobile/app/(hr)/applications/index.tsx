import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function HrApplicationsScreen() {
  return <PlaceholderScreen title="Hồ sơ ứng tuyển" note="Pipeline ứng viên — tuần 3–4." />;
}
