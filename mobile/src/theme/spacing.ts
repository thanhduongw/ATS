/** Port từ `frontend/src/app/theme.ts` (SPACING). Lưới 4 điểm, giống hệt web. */
export const SPACING = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  /** Lề mép màn hình. Web dùng 24 cho form đăng nhập ở mobile. */
  page: 24,
} as const;
